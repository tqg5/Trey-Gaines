/*
If any app wide css changes need to happen for the div it goes here.
Otherwise, use ./Wrapper.ts
*/
import styled from '@emotion/styled';

export default styled.div`
    height: 100%;
    background-color: #2c2d30;
`;